export interface DataSheet {
    height: number,
    width: number,
    depth: number,
    material: string
    finish: string,
    traffic: string,
    warranty: string
    connectionType: string
    technicalComment?: string
}