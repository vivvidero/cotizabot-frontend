export interface ApusTable {
    id: number;
    code: null;
    name: string;
    unit: string;
    totalValue: null | number;
    subCategory: string;
    unitPrice: string;
    references: string[];
}
